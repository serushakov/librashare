import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const Search = () => {
  return (
    <View style={styles.root}>
      <Text>No time to implement :(</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  root: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Search;
